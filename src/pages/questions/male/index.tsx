import { MASSIVE } from "constants/QuestionMassive";
import TryOut from "../female/tryout";
import { Box } from "components";
import React from "react";

console.log(MASSIVE);

const Male = () => {
  return (
    <Box>
      {/* {MASSIVE.map((object, i) => (
        <Box>
          {MASSIVE[i].map((value, i) => (
            <Box>{value}</Box>
          ))}
        </Box>
      ))} */}
    </Box>
  );
};

export default Male;
{
  /* <TryOut answers={title} key={id} link={idx}/>  */
}
{
  /* // { STEP_ONE, STEP_TWO, STEP_THREE, STEP_FOUR, STEP_FIVE, STEP_SIX } */
}
{
  /* // <TryOut
        //   question={object[i].question}
        //   answers={object}
        //   linkTo={object[i].navigatePage}
        // /> */
}
